// COMPATIBILITY: presentation bridge to the current RenderScene read-model adapter.
// REMOVE WHEN: family presentation receives the canonical viewer read-model contract directly.
export '../application/render_scene_models.dart';
