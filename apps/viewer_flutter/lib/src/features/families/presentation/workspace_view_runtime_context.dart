// COMPATIBILITY: local bridge to the legacy active-view runtime adapter.
// REMOVE WHEN: family overlay receives active-view state through composition.
export '../../../app/workspace/workspace_view_runtime_context.dart';
