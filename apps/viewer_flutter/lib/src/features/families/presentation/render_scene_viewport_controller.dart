// COMPATIBILITY: local bridge to the canonical viewer viewport controller.
// REMOVE WHEN: family overlay imports the viewer presentation owner directly.
export '../../viewer/presentation/viewport/render_scene_viewport_controller.dart';
